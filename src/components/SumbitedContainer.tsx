import styled from "@emotion/styled";
import PrimaryContainer from "./PrimaryContainer";

const SubmitContainer = styled(PrimaryContainer)`
  margin-top: 25px;
  display: flex;
  justify-content: flex-end;
  padding: 24px;
`;

export default SubmitContainer;
